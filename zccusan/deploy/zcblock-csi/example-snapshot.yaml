apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: zcfile-snap
  namespace: zcblock-demo
spec:
  volumeSnapshotClassName: zcblock
  source:
    persistentVolumeClaimName: zcfile-snap-source
