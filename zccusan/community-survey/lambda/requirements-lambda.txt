# PostgreSQL v3 wire-protocol driver for Aurora DSQL. Authentication uses the
# handler's lightweight SigV4 signer, so the deployment package does not need boto3.
psycopg[binary]==3.2.9
typing_extensions==4.14.1
